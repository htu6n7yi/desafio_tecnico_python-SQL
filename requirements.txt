fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-dotenv==1.0.0
mysql-connector-python==9.0.0
```

### 1.5 - Crie o arquivo `Procfile` na raiz:
```
web: uvicorn api:app --host 0.0.0.0 --port $PORT